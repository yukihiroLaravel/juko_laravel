# <画面名>

<!--
配置先は docs/specs/screens/<screen-name>.md である。
性質は状態を表す文書であり常に現行仕様へ上書き更新する。
書かないものはレイアウト・余白・色（Figmaの担当）と、
機械的な制約である必須・最大文字数・型（スキーマの担当）である。
書くものは振る舞い・状態・権限・遷移・業務判断が入るバリデーションである
-->

## 目的と利用者

<!-- 誰が何のために使う画面か1〜2文で書く -->

共通仕様は `docs/specs/screens/conventions.md` に従い、以下は差分のみを書く。

## 関連コンテキスト

<!--
必須である。画面名からは「注文」がどのコンテキストの注文か判別できない。
複数コンテキストが混ざる画面では、項目ごとにどちらの定義かを明記する
-->

`<context>`（ここでの「<用語>」は <context> の<定義>であり、`<他context>` の<別用語>ではない）

## パス

`/<path>`

## 表示項目

| 列 | 出典 | 備考 |
|---|---|---|
| <列名> | `<table>.<column>` | 表示形式・省略ルール・押下時の挙動 |

## 受け入れ基準

<!-- SHALL形式で書く。IDは AC-<画面略称>-NNN とする -->

1. `AC-XXX-001` 初期表示のとき、システムは<既定の並び順>で表示しなければならない。
2. `AC-XXX-002` <操作>されたとき、システムは<振る舞い>しなければならない。
3. `AC-XXX-003` <検索条件>は<範囲指定・完全一致・部分一致>とし、<境界条件>の場合は<扱い>としなければならない。
4. `AC-XXX-004` 複数の検索条件が指定されたとき、システムはANDで結合しなければならない。
5. `AC-XXX-005` 検索と並び替えの結果はサーバー側で処理しなければならない（ページ内のみの並び替えは不可である）。

<!--
AC-005 のような一見自明な項目を明示するのは、書かないとフロントで配列をsortする
実装が出て、ページネーションと組み合わせたときに壊れるためである
-->

## 状態別の挙動

<!-- Figmaに書けない領域であり、エージェントが最も勝手に埋める箇所である -->

| 状態 | 表示 |
|---|---|
| 0件 | |
| 読み込み中 | |
| エラー | |

## 画面遷移

- ← <遷移元>
- → <遷移先>（`<screen>.md`）

## 対象外

- <この画面ではやらないこと>（次期対応）

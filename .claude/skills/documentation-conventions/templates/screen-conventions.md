# 画面共通仕様

<!--
配置先は docs/specs/screens/conventions.md である。
個別画面の仕様はこのファイルからの差分のみを書く。
同じことを各画面に書くのは維持不可能なため、共通パターンはすべてここに集約する。
以下は項目立ての雛形であり、プロジェクトの実態に合わせて書き換える
-->

## 一覧画面

- ページネーションの件数と方式
- 検索・並び替え・ページングをサーバー側とクライアント側のどちらで処理するか
- 検索条件をURLクエリに保持するか
- 複数の検索条件をANDとORのどちらで結合するか
- 複数選択可の条件で、選択なしが何を意味するか
- テキスト検索を部分一致と完全一致のどちらとするか
- 0件時の表示

## フォーム

- 二重送信の防止
- バリデーションの実行タイミング
- サーバー側バリデーションエラーの表示位置
- 入力途中の離脱時の扱い

## 破壊的操作

- 確認ダイアログの要否
- 取り消し不可の操作の明示

## 権限による表示制御

- 権限がない機能を非表示とするかdisabledとするか
- 権限がない画面へ直接URLアクセスされた場合の挙動

## 表示形式

| 種別 | 形式 |
|---|---|
| 日付 | |
| 日時 | |
| 数値 | |
| 空値 | |

## 状態表示

- 読み込み中の表現
- エラー時の再試行手段
- 通信エラーとバリデーションエラーの表示位置の使い分け

## 用語

画面上の業務用語は `docs/domain/<context>/glossary.md` の表記に従う。複数コンテキストの情報を並べる画面では、どちらの定義かを画面上で判別できるようにする。

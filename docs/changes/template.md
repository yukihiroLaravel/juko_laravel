# <変更の要約>

<!--
配置先は docs/changes/<変更を表す名前>.md である。採番はしない。
ファイル名は英語の小文字とハイフンでつなぐ（例 instructor-list-student-count-deadline.md）。
一時的な文書であり、実装が終わったらこのファイルごと削除する。
恒久的に残す内容は恒久文書へ反映する。反映する時機は内容によって違う（下の「恒久文書への反映」を参照）。
テストの計画・テストケース・テストの命名や配置は書かない。
-->

## 目的

<!-- 何のためにこの変更を行うのか。1〜2文で書く -->

## 背景

<!-- なぜ今この変更が必要になったのか。関連する未確定事項があれば Q-NNN で参照する -->

## 変更する振る舞い

<!--
受け入れ基準の形で書く。既存の振る舞いを変える場合は既存の AC-ID を挙げ、
新しい振る舞いを足す場合は追加する ID を書く。
-->

| 対象 | 現在 | 変更後 |
|---|---|---|
| `AC-XXX-NNN` | | |

## 変更する範囲

<!-- 触る実装の範囲を書く。ファイル名の列挙ではなく、責務の単位で書く -->

| 範囲 | 内容 |
|---|---|
| エンドポイント | |
| 入力の検証 | |
| 業務処理 | |
| データ構造 | |

## 恒久文書への反映

<!--
反映が済んだ行にチェックを入れる。反映する時機は行ごとに違う。
着手の初期に反映するものは、テストの内容では覆らない判断だけである。
残りは実装が固まってから反映する。早く書くと、テストの承認で差し戻されたときに
誤った記述が恒久文書に残る。
-->

着手の初期に反映する。

- [ ] 業務ルールが判明・変更された → `docs/domain/shared/business-rules.md`（ID を維持して上書き）
- [ ] 用語が定まった → `docs/domain/shared/glossary.md`
- [ ] 未確定のまま進める点がある → `docs/domain/open-questions.md` に `Q-NNN` を追加し、コードに `TODO(Q-NNN)` を残す
- [ ] 技術選定の判断をした → `docs/adr/` に新しい記録を追加

実装が固まってから反映する。

- [ ] システムの振る舞いが決まった → `docs/specs/` の該当ファイル（ID を維持して上書き）
- [ ] データ構造・エンドポイントを変えた → `docs/architecture/`

## 対象外

<!-- この変更でやらないこと。書かないと過剰な実装が生まれる -->

## 完了の条件

- 変更した振る舞いが受け入れ基準のとおりに動作すること
- 既存のテストがすべて通ること
- 上の「恒久文書への反映」がすべて済んでいること
- この起票ファイルを削除すること
